# openfoam-bc-repair-40c6b98b

Synthetic OpenFOAM RL task generated by the root-level pipeline.

Family: `boundary_condition_repair`

The agent must repair the public starter case from `data/starter_case.tar.gz` and
write `/tmp/output/fixed_case.tar.gz`. The deterministic scorer applies hidden
variants and checks OpenFOAM-style structure, forbidden edits, solver health, and
engineering metrics.

Claude-hard rationale: Likely Claude-hard because the visible symptom can be patched several ways, but hidden flow reversal and viscosity variants require multi-file OpenFOAM BC consistency rather than a single text edit.
