# openfoam-flow-rate-a733a0f2

Synthetic OpenFOAM RL task generated by the root-level pipeline.

Family: `flow_rate_targeting`

The agent must repair the public starter case from `data/starter_case.tar.gz` and
write `/tmp/output/fixed_case.tar.gz`. The deterministic scorer applies hidden
variants and checks OpenFOAM-style structure, forbidden edits, solver health, and
engineering metrics.

Hidden validation axes: inlet_velocity, viscosity

Claude-hard rationale: Likely Claude-hard because the visible case runs, but hidden inlet-speed and viscosity variants require preserving the intended flow-control boundary rather than compensating elsewhere.
