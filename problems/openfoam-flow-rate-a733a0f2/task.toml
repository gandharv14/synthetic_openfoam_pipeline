schema_version = "1.1"

[task]
name = "labelbox/openfoam-flow-rate-a733a0f2"
description = "The case is stable but the inlet flow target is set far below the documented operating point."

[environment]
cpus = 4
memory_mb = 8192
storage_mb = 10000
gpus = 0
gpu_types = []
allow_internet = true

[agent]
timeout_sec = 1800

[verifier]
timeout_sec = 1200
env = []

[difficulty]
task_type = "cfd"
domain = "openfoam-repair"

[ground_truth]
in_container = true
render_command = "bash solution/render.sh"
render_outputs = [
  { path = "/tmp/output/rendering.mp4", required = false, description = "Optional synthetic OpenFOAM reviewer artifact" },
]

[[outputs]]
path = "/tmp/output/fixed_case.tar.gz"
required = true
description = "Corrected OpenFOAM case archive"
