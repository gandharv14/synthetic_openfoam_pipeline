# openfoam-force-coeff-c0ecb644

Synthetic OpenFOAM RL task generated by the root-level pipeline.

Family: `force_coeff_instrumentation`

The agent must repair the public starter case from `data/starter_case.tar.gz` and
write `/tmp/output/fixed_case.tar.gz`. The deterministic scorer applies hidden
variants and checks OpenFOAM-style structure, forbidden edits, solver health, and
engineering metrics.

Hidden validation axes: write_interval, probe_location

Claude-hard rationale: Likely Claude-hard because successful repair requires valid OpenFOAM function-object syntax, not just adding comments or post-hoc metric files.
