# openfoam-transient-2d2deab6

Synthetic OpenFOAM RL task generated by the root-level pipeline.

Family: `transient_setup_repair`

The agent must repair the public starter case from `data/starter_case.tar.gz` and
write `/tmp/output/fixed_case.tar.gz`. The deterministic scorer applies hidden
variants and checks OpenFOAM-style structure, forbidden edits, solver health, and
engineering metrics.

Hidden validation axes: write_interval, inlet_velocity

Claude-hard rationale: Likely Claude-hard because the correction is in run-control semantics rather than solver physics, and hidden write-cadence variants reject one-off timing edits.
